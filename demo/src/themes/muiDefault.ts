import { createTheme } from '@mui/material/styles';

// Pure MUI Default Theme - no customizations
// This serves as a baseline for comparing custom theme changes
export const muiDefaultTheme = createTheme();
